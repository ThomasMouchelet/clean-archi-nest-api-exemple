version: '3.1'
services:
  database:
    image: postgres
    container_name: postgres-worktransfer
    restart: always
    ports:
      - 5433:5432
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: test